# runtime state lives in data/ (ledger, archives, logs, pids, public URLs); a demo run regenerates what it needs
data/*
!data/.gitkeep
# the demo swaps these between v1/v2; the canonical sources are the .v1.jl/.v2.jl files
demo/app/actions/apply_adjustment.jl
demo/app/actions/transfer_funds.jl
*.tmp
media/frames/
media/frames.json
media/key-*.png
