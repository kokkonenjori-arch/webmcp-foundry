# runtime state (the ledger is data, not source; a demo run regenerates it)
data/*.jsonl
data/*.log
# demo transcript is regenerated by demo/run_demo.jl
data/demo-transcript.md
# the demo swaps these between v1/v2; the canonical sources are the .v1.jl/.v2.jl files
demo/app/actions/apply_adjustment.jl
demo/app/actions/transfer_funds.jl
*.tmp
data/public-*.txt
data/tunnel-*.log*
